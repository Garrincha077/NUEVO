name: GMLI GitHub Pages fetch-first fallback

on:
  push:
    branches: [main]
    paths:
      - 'index.html'
      - 'api/**'
      - 'lib/**'
      - 'scripts/build-pages-snapshot.mjs'
      - 'scripts/apply-decision-delta.mjs'
      - 'scripts/pages-decision-delta.mjs'
      - 'scripts/apply-liquidity-context.mjs'
      - 'scripts/pages-liquidity-context.mjs'
      - 'scripts/refresh-pages-inputs.py'
      - 'scripts/pages-context-history.mjs'
      - 'scripts/pages-money-ui.mjs'
      - 'scripts/pages-mobile-info.mjs'
      - 'scripts/pages-signal-role-ui.mjs'
      - 'scripts/pages-extremes-guide.mjs'
      - 'research/china-m2-official-v2/latest/**'
      - 'research/global-money-v2/latest/**'
      - 'research/global-money-v2/transfer/latest/**'
      - 'research/funding-v2/latest/**'
      - 'research/fiscal-v2/latest/**'
      - '.github/workflows/gmli-pages.yml'
  pull_request:
    branches: [main]
    paths:
      - 'index.html'
      - 'api/**'
      - 'lib/**'
      - 'scripts/build-pages-snapshot.mjs'
      - 'scripts/apply-decision-delta.mjs'
      - 'scripts/pages-decision-delta.mjs'
      - 'scripts/apply-liquidity-context.mjs'
      - 'scripts/pages-liquidity-context.mjs'
      - 'scripts/refresh-pages-inputs.py'
      - 'scripts/pages-context-history.mjs'
      - 'scripts/pages-money-ui.mjs'
      - 'scripts/pages-mobile-info.mjs'
      - 'scripts/pages-signal-role-ui.mjs'
      - 'scripts/pages-extremes-guide.mjs'
      - 'research/china-m2-official-v2/latest/**'
      - 'research/global-money-v2/latest/**'
      - 'research/global-money-v2/transfer/latest/**'
      - 'research/funding-v2/latest/**'
      - 'research/fiscal-v2/latest/**'
      - '.github/workflows/gmli-pages.yml'
  schedule:
    - cron: '20 6 * * *'
  workflow_dispatch:
  workflow_run:
    workflows: ['GMLI Funding V2 guarded refresh']
    types: [completed]
    branches: [main]

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: gmli-pages-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build:
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Validate fetch-first orchestrator
        run: python -m py_compile scripts/refresh-pages-inputs.py

      - name: Fetch and guarded-refresh Pages inputs
        if: github.event_name != 'pull_request'
        run: |
          set -euo pipefail
          python scripts/refresh-pages-inputs.py | tee /tmp/gmli-pages-refresh.json
          grep -Eq '"status": "PASS_FETCH_FIRST"|"status": "PASS_WITH_LAST_GOOD_FALLBACK"' /tmp/gmli-pages-refresh.json
          test -s audit/pages-refresh-status.json
          grep -q 'FETCH_FIRST_WITH_PER_LAYER_LAST_GOOD_FALLBACK' audit/pages-refresh-status.json

      - name: Build verified static GMLI snapshot
        run: |
          set -euo pipefail
          node scripts/build-pages-snapshot.mjs | tee /tmp/gmli-pages.json
          node scripts/apply-decision-delta.mjs | tee /tmp/gmli-decision-delta.json
          node scripts/apply-liquidity-context.mjs | tee /tmp/gmli-liquidity-context.json
          grep -q 'PASS_GITHUB_PAGES_SNAPSHOT' /tmp/gmli-pages.json
          grep -q 'PASS_GMLI_DECISION_DELTA' /tmp/gmli-decision-delta.json
          grep -q '"scoring_effect": "NONE"' /tmp/gmli-decision-delta.json
          grep -q '"automatic_weight_change": 0' /tmp/gmli-decision-delta.json
          grep -q '"pages_decision_brief_ui": true' /tmp/gmli-decision-delta.json
          grep -q 'PASS_GMLI_LIQUIDITY_CONTEXT' /tmp/gmli-liquidity-context.json
          grep -q '"version": "GMLI_LIQUIDITY_CONTEXT_V1"' /tmp/gmli-liquidity-context.json
          grep -q '"evidence_tier": "RESEARCH_DIAGNOSTIC"' /tmp/gmli-liquidity-context.json
          grep -q '"scoring_effect": "NONE"' /tmp/gmli-liquidity-context.json
          grep -q '"automatic_weight_change": 0' /tmp/gmli-liquidity-context.json
          grep -q '"methodology_effect": "NONE"' /tmp/gmli-liquidity-context.json
          grep -q '"pages_liquidity_context_ui": true' /tmp/gmli-liquidity-context.json
          grep -q 'GMLI_GLOBAL_MONEY_V2_PBOC_OFFICIAL' /tmp/gmli-pages.json
          grep -q 'GMLI_FISCAL_V2_DEFICIT_IMPULSE' /tmp/gmli-pages.json
          grep -q 'GMLI_SIGNAL_ROLE_TAXONOMY_V1' /tmp/gmli-pages.json
          grep -q 'GMLI_MONEY_HISTORICAL_EXTREMES_V1' /tmp/gmli-pages.json
          grep -q '"signal_role_scoring_effect": "NONE"' /tmp/gmli-pages.json
          grep -q '"money_extremes_scoring_effect": "NONE"' /tmp/gmli-pages.json
          grep -q '"fiscal_automatic_weight": 0' /tmp/gmli-pages.json
          grep -q '"report_schema": "gmli-report-v1.6"' /tmp/gmli-pages.json
          grep -q '"pages_context_ui": true' /tmp/gmli-pages.json
          grep -q '"pages_context_history_ui": true' /tmp/gmli-pages.json
          grep -q '"pages_money_extremes_ui": true' /tmp/gmli-pages.json
          grep -q '"pages_investor_guide": true' /tmp/gmli-pages.json
          grep -q '"context_funding_history_rows"' /tmp/gmli-pages.json
          grep -q '"context_fiscal_history_rows"' /tmp/gmli-pages.json
          grep -q '"context_market_history_rows"' /tmp/gmli-pages.json
          grep -q '"money_available_date"' /tmp/gmli-pages.json
          grep -q '"money_extremes_latest_month"' /tmp/gmli-pages.json
          grep -q '"fiscal_available_date"' /tmp/gmli-pages.json
          grep -q '"history_rows"' /tmp/gmli-pages.json
          grep -q '"static_api_files": 12' /tmp/gmli-pages.json
          test -s .pages/index.html
          for f in report radar history status decision current-market opportunity positioning money-nowcast context-history refresh-status money-extremes decision-delta liquidity-context; do
            test -s ".pages/api/${f}.json"
          done
          grep -q 'GMLI_FISCAL_V2_DEFICIT_IMPULSE' .pages/api/report.json
          grep -q 'GMLI_SIGNAL_ROLE_TAXONOMY_V1' .pages/api/report.json
          grep -q '"scoring_effect": "NONE"' .pages/api/report.json
          grep -q '"decision_delta"' .pages/api/report.json
          grep -q '"decision_brief"' .pages/api/report.json
          grep -q '"automatic_global_conviction_weight": 0' .pages/api/status.json
          grep -q '"fiscal_v2_automatic_weight": 0' .pages/api/decision.json
          grep -q '"schema_version": "gmli-pages-context-history-v1"' .pages/api/context-history.json
          grep -q '"historical_series": false' .pages/api/context-history.json
          grep -q 'research/funding-v2/latest/history.csv' .pages/api/context-history.json
          grep -q 'research/fiscal-v2/latest/history.csv' .pages/api/context-history.json
          grep -q '"schema_version": "gmli-pages-refresh-v1"' .pages/api/refresh-status.json
          grep -q 'LIVE_DURING_REPORT_BUILD' .pages/api/refresh-status.json
          grep -q '"schema_version": "gmli-money-extremes-v1"' .pages/api/money-extremes.json
          grep -q '"version": "GMLI_MONEY_HISTORICAL_EXTREMES_V1"' .pages/api/money-extremes.json
          grep -q '"evidence_tier": "RESEARCH_DIAGNOSTIC"' .pages/api/money-extremes.json
          grep -q '"scoring_effect": "NONE"' .pages/api/money-extremes.json
          grep -q '"automatic_weight_change": 0' .pages/api/money-extremes.json
          grep -q '"rolling_window_months": 120' .pages/api/money-extremes.json
          grep -q '"minimum_months": 36' .pages/api/money-extremes.json
          grep -q '"lookahead": false' .pages/api/money-extremes.json
          grep -q '"schema_version": "gmli-decision-delta-v1"' .pages/api/decision-delta.json
          grep -q '"schema_version": "gmli-decision-brief-v1"' .pages/api/decision-delta.json
          grep -q '"scoring_effect": "NONE"' .pages/api/decision-delta.json
          grep -q '"automatic_weight_change": 0' .pages/api/decision-delta.json
          grep -q 'RECONSTRUCTED_FIXED_RUBRIC_PROXY' .pages/api/decision-delta.json
          grep -q '"schema_version": "gmli-liquidity-context-v1"' .pages/api/liquidity-context.json
          grep -q '"version": "GMLI_LIQUIDITY_CONTEXT_V1"' .pages/api/liquidity-context.json
          grep -q '"evidence_tier": "RESEARCH_DIAGNOSTIC"' .pages/api/liquidity-context.json
          grep -q '"scoring_effect": "NONE"' .pages/api/liquidity-context.json
          grep -q '"automatic_weight_change": 0' .pages/api/liquidity-context.json
          grep -q '"methodology_effect": "NONE"' .pages/api/liquidity-context.json
          grep -q 'TLAACBW027SBOG' .pages/api/liquidity-context.json
          grep -q 'Monthly Statement of the Public Debt' .pages/api/liquidity-context.json
          grep -q 'Global Money Trend' .pages/index.html
          grep -q 'Money Historical Extremes' .pages/index.html
          grep -q 'moneyLevelZChart' .pages/index.html
          grep -q 'moneyAccelZChart' .pages/index.html
          grep -q 'Kako čitati GMLI — vodič za investitore' .pages/index.html
          grep -q 'id="investorGuide"' .pages/index.html
          grep -q 'id="decisionBrief"' .pages/index.html
          grep -q 'WHAT CHANGED' .pages/index.html
          grep -q 'DECISION</a>' .pages/index.html
          grep -q 'id="contextLayers"' .pages/index.html
          grep -q 'Context History' .pages/index.html
          grep -q 'contextFundingHistoryChart' .pages/index.html
          grep -q 'contextFiscalHistoryChart' .pages/index.html
          grep -q 'contextMarketHistoryChart' .pages/index.html
          grep -q 'FISCAL V2' .pages/index.html
          grep -q 'SIGNAL ROLE CHAIN' .pages/index.html
          grep -q 'CONTEXT</a>' .pages/index.html
          grep -q 'EXTREMES</a>' .pages/index.html
          grep -q 'GUIDE</a>' .pages/index.html
          grep -q 'moneyScoreChart' .pages/index.html
          grep -q 'moneyYoyChart' .pages/index.html
          grep -q 'infoPopover' .pages/index.html
          grep -q 'Više informacija' .pages/index.html
          grep -q 'Refresh audit' .pages/index.html
          grep -q 'id="liquidityContext"' .pages/index.html
          grep -q 'LIQUIDITY CONTEXT</a>' .pages/index.html
          grep -q 'Bank balance-sheet impulse' .pages/index.html
          grep -q 'Treasury duration mix' .pages/index.html
          grep -q 'liquidity-context.json' .pages/index.html
          if grep -q 'gmli-fred-dashboard.vercel.app/api/' .pages/index.html; then
            echo 'Pages still depends on Vercel API links' >&2
            exit 1
          fi

      - name: Publish verified static snapshot to gh-pages branch
        if: github.event_name != 'pull_request'
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          work="$(mktemp -d)"
          cp -a .pages/. "$work/"
          cd "$work"
          git init -q
          git config user.name 'gmli-pages-bot'
          git config user.email 'gmli-pages-bot@users.noreply.github.com'
          git checkout -q -b gh-pages
          git add -A
          git commit -q -m "Publish fetch-first verified GMLI Pages snapshot ${GITHUB_SHA}"
          git remote add origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push -q --force origin HEAD:gh-pages
          echo "Fetch-first static snapshot published to gh-pages branch."

      - name: Configure Pages
        if: github.event_name != 'pull_request'
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v4
        with:
          path: .pages

  deploy:
    if: github.event_name != 'pull_request'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
