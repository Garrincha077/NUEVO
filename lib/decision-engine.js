import { FROZEN_STATE, regimeFromScore } from './state.js';
import { getLiveMoneyNowcast, summarizeLiveNowcast, moneyNowcastFreshness } from './live-money-nowcast.js';
import { yahooMonthly, priceTurn } from './opportunity-data.js';
function coreMoneyAgreement(){const u=regimeFromScore(FROZEN_STATE.money.usd_score),f=regimeFromScore(FROZEN_STATE.money.fxn_score);return{usd_regime:u,fx_neutral_regime:f,agreement:u===f?'AGREE':'DIVERGENT'};}
async function coreMarketConfirmationFromOpportunity(opportunity){if(opportunity?.assets){const rows=['SPY','QQQ','GLD','DBC'].map(k=>opportunity.assets[k]?.entry_inputs?.turn).filter(x=>x?.available);const positive=rows.filter(x=>x.pass).length;return{available:rows.length,positive,total:rows.length,score_0_2:rows.length>=3?(positive>=3?2:positive===2?1:0):1,basis:'completed-month price turn across SPY/QQQ/GLD/DBC'};}const rows=[];for(const k of ['SPY','QQQ','GLD','DBC']){try{rows.push(priceTurn(await yahooMonthly(k)));}catch{}}const positive=rows.filter(x=>x.pass).length;return{available:rows.length,positive,total:rows.length,score_0_2:rows.length>=3?(positive>=3?2:positive===2?1:0):1,basis:'completed-month price turn across SPY/QQQ/GLD/DBC'};}
export async function buildDecision(opportunity=null){const liveMoney=await getLiveMoneyNowcast();const nowcast=summarizeLiveNowcast(liveMoney);const fundingScore=FROZEN_STATE.funding.score;const fundingRegime=fundingScore<40?'RESTRICTIVE':fundingScore>60?'SUPPORTIVE':'NEUTRAL';const agreement=coreMoneyAgreement();const market=await coreMarketConfirmationFromOpportunity(opportunity);const rubric={money_freshness:{score:FROZEN_STATE.money.freshness==='STALE'?0:2,max:2,reason:FROZEN_STATE.money.freshness==='STALE'?`Frozen Core dated ${FROZEN_STATE.money.available_date}`:'Frozen Core current'},usd_fxn_agreement:{score:agreement.agreement==='AGREE'?2:0,max:2,reason:`${agreement.usd_regime} vs ${agreement.fx_neutral_regime}`},transmission_evidence:{score:2,max:2,reason:'Six frozen/promoted asset-transmission relationships remain the primary allocation priors'},funding_confirmation:{score:fundingRegime==='SUPPORTIVE'?2:fundingRegime==='NEUTRAL'?1:0,max:2,reason:`Funding ${fundingRegime.toLowerCase()} (${fundingScore.toFixed(1)})`},market_confirmation:{score:market.score_0_2,max:2,reason:`${market.positive}/${market.total||4} core assets have positive completed-month turn`}};const convictionScore=Object.values(rubric).reduce((a,x)=>a+x.score,0);return{schema_version:'gmli-decision-v3',engine_version:'GMLI 2.3',as_of:new Date().toISOString().slice(0,10),regime:{label:'NEUTRAL',score:null,score_status:'NOT_COMPUTED',provisional:true,tilt:'MILD_POSITIVE',basis:'MONEY_CORE_FIRST_WITH_CURRENT_RESEARCH_CONFIRMATION',note:'No synthetic USD/FX-neutral composite Core score. USD and FX-neutral are interpreted separately.'},conviction:{score:convictionScore,max:10,label:`${convictionScore}/10`,rubric},money:{status:FROZEN_STATE.money.status,usd_score:Number(FROZEN_STATE.money.usd_score.toFixed(1)),usd_regime:agreement.usd_regime,fx_neutral_score:Number(FROZEN_STATE.money.fxn_score.toFixed(1)),fx_neutral_regime:agreement.fx_neutral_regime,agreement:agreement.agreement,available_date:FROZEN_STATE.money.available_date,freshness:FROZEN_STATE.money.freshness},money_nowcast:{status:liveMoney.evidence_tier,tilt:nowcast.tilt,coverage:nowcast.coverage,accelerating:`${nowcast.accelerating}/4`,expanding_yoy:`${nowcast.expanding_yoy}/4`,details:'/api/money-nowcast',current_research_inference:liveMoney.interpretation.current_research_inference},funding:{status:'OVERLAY',regime:fundingRegime,score:Number(fundingScore.toFixed(1)),available_date:FROZEN_STATE.funding.available_date},market_confirmation:{status:'RESEARCH',...market},opportunity_overlay:{status:'RESEARCH',details:'/api/opportunity',method:'Strategic Eligibility first; Entry Quality second.'},freshness:{core_money:FROZEN_STATE.money.available_date,money_nowcast:moneyNowcastFreshness(liveMoney.blocks),overlays:FROZEN_STATE.funding.available_date,warning:'Live freshness improves current inference but does not replace frozen Core.'},evidence_tiers:{money:'CORE',money_nowcast:'RESEARCH',funding:'OVERLAY',credit:'OVERLAY',fiscal:'OVERLAY'},key_reasons:['Frozen Money Core is divergent: USD Risk-On, FX-neutral Neutral.',`Freshness overlay is ${nowcast.tilt}: ${nowcast.accelerating}/4 blocks accelerated and ${nowcast.expanding_yoy}/4 are expanding YoY.`,`Funding remains ${fundingRegime.toLowerCase()}, limiting conviction.`,`${market.positive}/${market.total||4} primary market turns are currently positive.`],invalidation_triggers:['Fresh broad-money data deteriorate across multiple major blocks.','FX-neutral Money weakens further while the USD translation headwind rises.','Market confirmation turns broadly negative across the promoted transmission assets.']};}
